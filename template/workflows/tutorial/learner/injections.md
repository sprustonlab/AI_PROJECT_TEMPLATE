# Injections Phase

In this phase you'll see how workflow injections modify tool inputs.

Try running `echo hello` — the injection rule will append text to the command automatically.
Injections are phase-scoped, so this only happens in this phase.

To advance: create `tutorial_injections_done.txt` then call `advance_phase`.
