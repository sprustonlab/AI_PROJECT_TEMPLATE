# User Prompt

Update the docs on this repo. A lot has changed — there are hints, advance checks, rules, phases, workflows, and chicsessions. This is not in the readme files and not accessible to agents helping build this framework.

We should also include `.claude/rules/` files so agents get context when they read/write to locations related to these systems.

The main focus is the systems in `submodules/claudechic/` that we use in the template.
