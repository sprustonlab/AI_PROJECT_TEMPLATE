# Basics Phase

Welcome! In this phase you'll learn:
1. **Workflow activation** — This workflow was activated via `/tutorial`
2. **Phases** — You're in phase 1 of 4. Check with the `get_phase` MCP tool.
3. **Hints** — You should see toast notifications guiding you.
4. **Injections** — Try `echo hello` and notice the injected comment.

To advance: create `tutorial_basics_done.txt` then call `advance_phase`.
